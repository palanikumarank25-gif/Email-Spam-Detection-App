apiVersion: v1

kind: Service

metadata:
  name: spam-prediction-service

spec:
  selector:
    app: spam-prediction

  type: NodePort

  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30007